import LoginPage from "@/pages/Login"

const Login = () => {
  return (
    <div className=" bg-primary items-center justify-center  p-6 lg:p-10 flex  grow">
      {<LoginPage />}
    </div>
  );
}

export default Login